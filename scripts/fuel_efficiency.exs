# This script has been moved to a Mix task for cleaner execution (no DB log noise).
#
# Usage:
#   mix fuel_efficiency
#
# Source: lib/mix/tasks/fuel_efficiency.ex
raise "Use `mix fuel_efficiency` instead of `mix run scripts/fuel_efficiency.exs`"
